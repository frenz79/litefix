package com.litefix.warmup;

import com.litefix.commons.utils.MathUtils;

public class MathUtilsWarmup {

	public static void warmup( int counter ) {		
		for( int i=0; i<counter; i++ ) {
			MathUtils.getDigits(i);
		}
	}
	
}
